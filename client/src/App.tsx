import React from 'react';
import PagesList from "./compotents/PagesList";

function App() {
    return (
        <div style={{display: "flex", justifyContent: "space-around"}} >
            <PagesList/>
        </div>
    );
}

export default App;
